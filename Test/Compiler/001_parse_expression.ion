var x: int = 6;
var y: int = 4 + 3;
var z: string = "12" + "testing"; // type inference to string during typechecking I actually patch the ast

struct UserDefined {
    var a: int;
};

func do_something() -> int {
    return 54;
}

func main() -> void {
    var t := do_something();
    // o = "testing";

    // var o: int; // Also typechecker error... [Line: 11 | Duplicate variable decleration: o]
    // t = 3; // [Error Line: 13 | Undeclared identifier 't']
    //o = "hello world"; // [Error Line: 15 | Assignment of int to string is not allowed!]
}

